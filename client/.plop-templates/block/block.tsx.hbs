import { ReactNode } from "react";
import { mash } from "../../../utils/class-mash";
import { Box, type BoxProps } from "../box";

import styles from "./{{kebabCase name}}.module.scss";

export interface {{pascalCase name}}Props extends BoxProps {
  children: ReactNode[] | ReactNode;
}

export const {{pascalCase name}} = mash<{{pascalCase name}}Props>(({ children }) => {
  return <div>{children}</div>;
}, styles.{{camelCase name}});
